<header class="masthead mb-auto">
  <div class="inner">
    <h3 class="masthead-brand">Terence Boyle</h3>
    <nav class="nav nav-masthead justify-content-center">
      <%= link_to "Home", root_path, { :class=>"nav-link #{nav_active 'home'}" }%>
      <%= link_to "About Me", about_path, { :class=>"nav-link #{nav_active 'about'}" } %>
      <%= link_to "Contact", contact_path, { :class=>"nav-link #{nav_active 'contact'}" }%>
      <%= link_to "Blog", blogs_path, { :class=>"nav-link" }%>
      <%= link_to "Portfolio", portfolios_path, { :class=>"nav-link" }%>
      <%= login_helper 'nav-link' %>
    </nav>
  </div>
</header>
